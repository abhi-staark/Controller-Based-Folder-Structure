/// This file contains size constants such as paddings, margins, and font sizes.
/// Using these constants ensures consistency across the app’s UI
/// and makes it easy to adjust sizes globally if needed.

class AppSizes {
  static const double buttonHeight = 50.0;           // Standard button height
  static const double cardElevation = 4.0;           // Standard card elevation
  static const double padding = 16.0;                // Standard padding
  // Add more size constants as needed
}
